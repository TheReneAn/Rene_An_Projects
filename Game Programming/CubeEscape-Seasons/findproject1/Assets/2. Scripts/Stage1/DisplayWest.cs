using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisplayWest : DisplayBase
{
    // Start is called before the first frame update
    void Start()
    {
        category = DisplayCategory.West;
        InitDisplay();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
